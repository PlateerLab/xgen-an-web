"""
Core control plane for AN-Web.

Modules:
    engine    - Main engine entry point (ANWebEngine)
    session   - Session lifecycle management (Session)
    scheduler - asyncio-based task/micro/macro scheduler (EventLoopScheduler)
    state     - Engine global state (EngineState)
    snapshot  - Deterministic snapshot management (SnapshotManager)
"""
